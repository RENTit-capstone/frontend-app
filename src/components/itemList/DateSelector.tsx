import { Text, View } from "react-native";

const DateSelector = () => {
    return (   
        <View>
            <Text>
                일정 선택
            </Text>
        </View>
    );
}

export default DateSelector;