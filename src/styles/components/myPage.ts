import { StyleSheet } from 'react-native';

export const myPage = StyleSheet.create({
    otpBox: {
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 6,
        paddingVertical: 30,
    },
});
